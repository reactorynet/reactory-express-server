# host server
$tableSpace1 = @db("connection_id", "dbo.Table");
$tableSpace2 = @db("connection_id2", "OtherTable");
$results = [];

declare search {
  $results = select id from $tableSpace1 where $var.name = $name;
  $results += select id from $tableSpace2 where $var.name = $name; 
}

@search