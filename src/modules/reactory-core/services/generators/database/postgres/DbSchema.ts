import postgres from 'postgres';
/**
 * The Db schema is a service class that provides meta data about the 
 * database schema. The schema is used to generate forms for the user
 */

class PostgresSchemaProvider {

  getSchemas(connection: any ) {

  }

}