/**
* Module generated by Reactory Module Generator.
* @nameSpace <%= module.nameSpace %>
* @name <%= module.name %>
* @version <%= module.version %>
* @description <%= module.description %>
*/
import Reactory from '@reactory/reactory-core';
import graphDefinitions from './graphql';
import workflows from './workflow';
import cli from './cli';
import service from './services';
import models from './models';
import forms from './forms';
import translations from './translations';
import clientPlugins from './clientPlugins';
import passportProviders from './passportProviders';
import pdfs from './pdfs';
import cli from './cli';

const module: Reactory.Server.IReactoryModule = {
  nameSpace: <%= module.namespace %>,
  version: <%= module.version %>,
  name: <%= module.name %>,
  description: <%= module.description %>,
  dependencies: [<%= module.dependencies %> ],
  priority: <%= module.priority %>,
  graphDefinitions,
  workflows,
  forms,
  services,
  translations,
  models,
  clientPlugins,
  passportProviders,
  cli,
  pdfs,
};

export default module;
