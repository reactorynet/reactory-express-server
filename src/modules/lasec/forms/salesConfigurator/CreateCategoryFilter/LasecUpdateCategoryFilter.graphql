mutation LasecUpdateCategoryFilter($id: String, $input: LasecCategoryFilterCaptureInput!) {
  LasecUpdateCategoryFilter(id: $id, input: $input) {
     id
    title
    filterOptions {
      id
      key
      text
      value
    }
    key
    selectMultiple
  }
}
