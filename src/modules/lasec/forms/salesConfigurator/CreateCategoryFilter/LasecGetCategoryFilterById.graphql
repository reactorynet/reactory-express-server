query LasecGetCategoryFilterById($id: String!) {
  LasecGetCategoryFilterById(id: $id) {
    id
    title
    filterOptions {
      id
      key
      text
      value
    }
    key
    selectMultiple
  }
}
