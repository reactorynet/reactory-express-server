mutation LasecCreateNewCategory($input: CreateContentInput!){
  LasecCreateNewCategory(input: $input){
    id
    parentId
    children
    items
    linkType
    linkTarget
    foreignResolver
    key
    name
    description
  }
}
